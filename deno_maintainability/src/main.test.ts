import { assert } from "https://deno.land/std@0.50.0/testing/asserts.ts";

/***
 * Behaviour of main sum application
 *
 * TODO: Find out how to stub dependency
 */
Deno.test("sum on zero arguments - negative response", () => {
  assert(
    "Need to be updated as currently does not know how to mock dependency"
  );
});
